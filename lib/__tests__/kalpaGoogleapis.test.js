const assert = require('assert');
const kalpaGoogleapis = require('../index.js');

describe('kalpaGoogleapis', () => {
  it('has a test', () => {
    assert(false, 'kalpaGoogleapis should have a test');
  });
});
